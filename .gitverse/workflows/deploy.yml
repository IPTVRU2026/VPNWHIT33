stages:
  - deploy

pages:
  stage: deploy
  image: alpine:latest

  script:
    - apk add --no-cache sed
    - sed -i "s/__GITVERSE_TOKEN__/${GITVERSE_TOKEN}/g" index.html
    - mkdir public
    - cp index.html public/

  artifacts:
    paths:
      - public

  only:
    - master
